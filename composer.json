{
  "name": "swipe/swipe-images",
  "description": "WebP/AVIF beim Upload, Qualitätsregler, Migration. swipe GmbH.",
  "type": "wordpress-plugin",
  "license": "GPL-2.0-or-later",
  "require": { "php": ">=8.1" },
  "require-dev": {
    "phpunit/phpunit": "^9.6",
    "brain/monkey": "^2.6",
    "mockery/mockery": "^1.6"
  },
  "autoload": { "classmap": ["includes/", "admin/"] },
  "scripts": {
    "test": "phpunit --testdox",
    "lint": "! find . -name '*.php' -not -path './vendor/*' -print0 | xargs -0 -n1 php -l 2>&1 | grep -v 'No syntax errors' | grep ."
  },
  "config": { "platform": { "php": "8.1" } }
}
